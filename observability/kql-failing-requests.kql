// KQL: Failing Requests in Container App
AppTraces
| where SeverityLevel >= 3
| where Message has "fail" or Message has "error"
| project TimeGenerated, Message, SeverityLevel, OperationName, CloudRoleInstance
| order by TimeGenerated desc
